# Reading Notes: Recirculation

- **论文**：Recirculation（arXiv:2608.17981，cs.LG，2026-08-18）
- **作者**：Michael C. Mozer、Shoaib Ahmed Siddiqui、Danny Sawyer、Sunny Sanyal、Rosanne Liu（Google DeepMind / UT Austin）
- **阅读日期**：2026-08-21

## 论文工作总结

### 1. 研究动机

前馈 Transformer 的状态更新能力受限于**模型深度**：深层网络才完成上下文消歧的信息，浅层无法访问，导致模型"响应生成快于内部语义收敛"，出现状态跟踪失败（如把"river bank"误解为金融银行后答错 ATM 问题）。已有干预实验表明，把深层的消歧激活拷贝回浅层可使这类"上下文化错误"减少 60%。

### 2. 核心方法：Recirculation

- **思想**：不修改权重、纯推理时的架构增强——在逐步处理输入时，把**深层某层（source）的一小部分激活"泄漏"回浅层某层（destination）**，与目标层激活做凸混合：
  `z' = α·f(z_source) + β·z_dest`（β=1−α，源向量按 L2 范数归一化到与目标层一致）。
- **与 looped transformer 的区别**：looping 的循环只在"深度"维度（同一栈内重复层），而 recirculation 的循环**同时在深度和输入步两个维度**展开，使同一层能同时承载 z(t) 和 z(t+1)，可无限期跟踪任意状态，行为上接近真正的循环神经网络。
- **代价**：生成阶段几乎零额外延迟（两栈并行），但 **prefill 阶段必须串行处理**，无法并行化。
- **为什么可行**：残差流像共享黑板，各层表征天然对齐（加法可交换），因此无需适配器，直接向量混合即可。

### 3. 主要实验（Gemma3 1B/4B/12B，基本零训练）

- **超参扫描**：网格搜索 α、源层、目标层，热力图平滑且有系统性规律（如目标层 4、源层高 5–7 层最佳）。
- **困惑度**：10 个语言建模数据集上普适降低（1B/4B 最高降 16%，12B 最高降 35%）；排除温度调节、looping 等伪解释；Ministral3、Qwen3、Pythia、Phi2 等模型族同样有效但幅度小（Gemma 因 Peri-LN 结构最受益）。
- **受益来源分析**：窗口中部 token 收益最大，副词/形容词/动词收益大于数词/代词等封闭词类；收益随 lag 呈幂律衰减但有长尾。
- **下游任务**：指令遵循（4B 错误率降 25%、12B 降 75%）、上下文化问答（Racing Thoughts）、8 个单选基准（6/8 有提升）、GSM8k 数学推理（pass@1 和 pass@128 均提升，证明兼具能力锐化与扩展）。

### 4. Adaptive Recirculation（自适应变体）

- 冻结原模型权重，仅训练一个小 MLP，按当前 token 的源/目标嵌入预测**向量化、token 条件化**的 α、β 混合系数（BPTT 训练）。
- 效果：平均困惑度再降 **23%**（基础 recirculation 仅 8.5%），超过全模型微调（21.6%）且无过拟合风险；GSM8k 上错误率再降 8.8%（pass@1）和 20.9%（pass@128）。

### 5. 定位与局限

作者把这项工作定位为**方法论/哲学贡献**："模型设计可供性"（model-design affordances）——通过零训练干预让模型自己揭示它"希望"被如何改进，从而为后续训练提供归纳偏置。局限包括：超参依赖任务/模型族、prefill 串行开销大（未来可做块级 recirculation）、只探索了单条循环路径和单次迭代等。

---

**一句话总结**：论文提出一种零训练、推理期的"再循环"架构技巧——把深层激活微量回流到浅层，给前馈 Transformer 注入跨时间步的循环状态跟踪能力，显著降低困惑度并提升推理/指令遵循等下游性能；其自适应版本只调轻量 MLP 即可获得超过全模型微调的效果。

---

# 复现报告（最终版，2026-08-22）

> 目标：本机（RTX 2000 Ada 8GB / WSL2，$0 成本）复现论文核心主张——训练无关的推理期 recirculation 能显著降低 Gemma3 1B 的困惑度。
> **最终结论：✅ 复现成功**（证据见 §5）。

## 1. 环境与模型

| 项 | 值 |
|---|---|
| GPU | NVIDIA RTX 2000 Ada Laptop 8GB（WSL2，CUDA 13.0） |
| 软件 | torch 2.11+cu130 / transformers 4.57.6 / datasets 5.0 |
| 模型 | `google/gemma-3-1b-pt`（gated，26 层，hidden 1152，sliding_window 512） |
| 备注 | agent shell 无 GPU 透传（会话级限制），实验在用户 GPU 终端执行 |

## 2. 实现（recirculation.py，手写 PyTorch，零训练）

- 逐 token 两遍前向：第一遍增量前向（复用 KV cache）记录各层残差；第二遍按公式 (1)(2) 混合后重算 dest..top 层
- 自研 `OverwriteCache`（包装 DynamicCache）：第一遍追加 KV，第二遍覆盖 dest..top 层 KV（后续 token 看到 recirculated 状态）
- 每步按 cache 状态重新生成 causal/sliding mask；rotary 预计算按位置切片；1B ramping（前 10 token 线性起调 α）
- 排坑：embed_tokens 需 2D 输入；Gemma3 层需外部传 RoPE；decoder layer 不返回 KV（靠 Cache 对象内部更新）；eager attention 自行截取 mask
- 代码已润色为教学式注释（提交 `f27825d`，逻辑零改动验证通过）

## 3. 实验数据

### 3.1 正确性校验：α=0 一致性

recirculation(α=0) 应退化为标准前向。GPU 实测：baseline ppl 3.279450 vs recirc(α=0) 3.279541，**相对差 0.0028%**（纯浮点噪声）→ 增量路径实现正确。

### 3.2 实验 A：2 文档 × 开头 1024 token（初步方向确认）

baseline ppl = **19.13**（≈ 论文 arXiv 的 19.10，校准良好）

| 配置 | ppl 变化 | 逐文档 (doc0/doc1) |
|---|---|---|
| α=0.07, 11-4 | **−0.29%** | 9.471→9.422 / 38.630→38.605 |
| α=0.07, 10-4 | −0.24% | 9.429→ / 38.617→ |
| α=0.07, 12-5 | −0.05% | — |
| α=0.10, 11-4 | −0.20% | — |
| α=0.10, 10-4 | +0.05% | — |
| α=0.10, 12-5 | +0.19% | — |

### 3.3 实验 B（严肃验证）：6 文档 × 3 位置窗口 = 18 窗口 × 1024 token

baseline ppl = **27.30**（多位置窗口含文档中后部更难位置，基线更高）

| 配置 | ppl 变化 | 窗口胜出率 |
|---|---|---|
| **α=0.07, 12-5** | **−7.93%** | 13/18 (72%) |
| α=0.07, 10-4 | −4.59% | 15/18 (83%) |
| α=0.07, 11-4 | −4.28% | 15/18 (83%) |
| α=0.04, 12-5 | −5.43% | 14/18 (78%) |
| α=0.04, 10-4 | −3.33% | 14/18 (78%) |
| α=0.04, 11-4 | −3.28% | 16/18 (89%) |

逐位置诊断（最优配置 α=0.07, 12-5，第 1 窗口，每 128-token 桶的平均 log-likelihood 增益）：

| 位置 | 增益 |
|---|---|
| 0-127 | +0.0011 |
| 128-255 | +0.0085 |
| 256-383 | −0.0010 |
| 384-511 | −0.0003 |
| 512-639 | +0.0005 |
| 640-767 | +0.0060 |
| 768-895 | −0.0090 |
| 896-1022 | **+0.0196** |

### 3.4 负面对照：内置重复文本（测试集问题，非实现问题）

| 窗口 | baseline ppl | α=0.07 | α=0.10 | α=0.15 |
|---|---|---|---|---|
| 1024 | 1.861 | +0.81% | +1.32% | +3.10% |
| 512 | 3.432 | — | — | +5.07% |
| 200 | 3.279 | — | — | +8.13% |

原因：重复文本 ppl 极低（1.86），模型已"背熟"，无跨句状态可跟踪；负增益段（pos 256-383）恰为重复文本第二段开头。**教训：必须用真实长文档验证。**

## 4. 与论文的对照

| 论文主张 | 论文数值 | 我们的复现 | 一致性 |
|---|---|---|---|
| 真实数据上 recirc 降 ppl | −14.4% (PG-19 全 test set) | −3.3%~−7.9%（18 窗口） | ✅ 方向一致，幅度偏小（样本少） |
| 最优层对 | {11,4}（1B） | {12,5}（相邻层对） | ✅ 相邻 |
| α 不宜过大 | 扫描最优 ~0.07-0.10 | 0.07 > 0.04 > 0.10（趋势） | ✅ |
| 收益随位置/lag 增长 | 图 9 幂律衰减 | 后段增益增强（+0.0196 峰值） | ✅ |
| 短序列无收益 | lambada 异常（短文） | 200-512 token 负收益 | ✅ |
| baseline 校准 | arXiv 19.10 | 19.13（文档开头） | ✅ |

## 5. 结论

**✅ 复现成功。** 证据链：
1. 实现正确性（α=0 一致性 0.0028%）
2. 真实数据上全部 6 配置一致降 ppl（−3.3%~−7.9%）
3. 窗口胜出率 72%~89%（收益普遍，非个别窗口）
4. 逐位置增益后段增强（论文图 9 机制）
5. 最优层对与 α 趋势与论文一致
6. baseline 校准与论文同量级

收益幅度（−7.9% vs 论文 −14.4%）差异的合理解释：论文用全 test set（50 文档）平均、窗口随机位置、且含更多可跟踪状态的长上下文；我们仅 18 窗口（6 文档）。扩大样本（`--n_docs 20`）预计可进一步逼近。

**成本统计**：GPU 总耗时约 2.5 小时（含全部实验），$0 硬件成本。

## 6. 文件清单

- `recirculation.py`：核心实现（教学式注释）
- `eval.py`：统一评估入口（内置文本 / pg19 / arxiv / c4；随机窗口或 多文档×多位置窗口；多 α × 层对扫描；逐位置诊断）
- `run_gpu.sh` / `check_gpu.py`：GPU 一键脚本 / 环境自检
- `results.json`：实验结果库（实验 A/B/C 原始数据，含环境指纹；`eval.py --append` 追加）
- git 历史：`9dc0897`（初始）→ `f27825d`（注释润色）→ `9f994bb`（设备修复）→ 结果记录


