# Reading Notes: Recirculation

- **论文**：Recirculation（arXiv:2608.17981，cs.LG，2026-08-18）
- **作者**：Michael C. Mozer、Shoaib Ahmed Siddiqui、Danny Sawyer、Sunny Sanyal、Rosanne Liu（Google DeepMind / UT Austin）
- **阅读日期**：2026-08-21

## 论文工作总结

### 1. 研究动机

前馈 Transformer 的状态更新能力受限于**模型深度**：深层网络才完成上下文消歧的信息，浅层无法访问，导致模型"响应生成快于内部语义收敛"，出现状态跟踪失败（如把"river bank"误解为金融银行后答错 ATM 问题）。已有干预实验表明，把深层的消歧激活拷贝回浅层可使这类"上下文化错误"减少 60%。

### 2. 核心方法：Recirculation

- **思想**：不修改权重、纯推理时的架构增强——在逐步处理输入时，把**深层某层（source）的一小部分激活"泄漏"回浅层某层（destination）**，与目标层激活做凸混合：
  `z' = α·f(z_source) + β·z_dest`（β=1−α，源向量按 L2 范数归一化到与目标层一致）。
- **与 looped transformer 的区别**：looping 的循环只在"深度"维度（同一栈内重复层），而 recirculation 的循环**同时在深度和输入步两个维度**展开，使同一层能同时承载 z(t) 和 z(t+1)，可无限期跟踪任意状态，行为上接近真正的循环神经网络。
- **代价**：生成阶段几乎零额外延迟（两栈并行），但 **prefill 阶段必须串行处理**，无法并行化。
- **为什么可行**：残差流像共享黑板，各层表征天然对齐（加法可交换），因此无需适配器，直接向量混合即可。

### 3. 主要实验（Gemma3 1B/4B/12B，基本零训练）

- **超参扫描**：网格搜索 α、源层、目标层，热力图平滑且有系统性规律（如目标层 4、源层高 5–7 层最佳）。
- **困惑度**：10 个语言建模数据集上普适降低（1B/4B 最高降 16%，12B 最高降 35%）；排除温度调节、looping 等伪解释；Ministral3、Qwen3、Pythia、Phi2 等模型族同样有效但幅度小（Gemma 因 Peri-LN 结构最受益）。
- **受益来源分析**：窗口中部 token 收益最大，副词/形容词/动词收益大于数词/代词等封闭词类；收益随 lag 呈幂律衰减但有长尾。
- **下游任务**：指令遵循（4B 错误率降 25%、12B 降 75%）、上下文化问答（Racing Thoughts）、8 个单选基准（6/8 有提升）、GSM8k 数学推理（pass@1 和 pass@128 均提升，证明兼具能力锐化与扩展）。

### 4. Adaptive Recirculation（自适应变体）

- 冻结原模型权重，仅训练一个小 MLP，按当前 token 的源/目标嵌入预测**向量化、token 条件化**的 α、β 混合系数（BPTT 训练）。
- 效果：平均困惑度再降 **23%**（基础 recirculation 仅 8.5%），超过全模型微调（21.6%）且无过拟合风险；GSM8k 上错误率再降 8.8%（pass@1）和 20.9%（pass@128）。

### 5. 定位与局限

作者把这项工作定位为**方法论/哲学贡献**："模型设计可供性"（model-design affordances）——通过零训练干预让模型自己揭示它"希望"被如何改进，从而为后续训练提供归纳偏置。局限包括：超参依赖任务/模型族、prefill 串行开销大（未来可做块级 recirculation）、只探索了单条循环路径和单次迭代等。

---

**一句话总结**：论文提出一种零训练、推理期的"再循环"架构技巧——把深层激活微量回流到浅层，给前馈 Transformer 注入跨时间步的循环状态跟踪能力，显著降低困惑度并提升推理/指令遵循等下游性能；其自适应版本只调轻量 MLP 即可获得超过全模型微调的效果。

---

## 复现阶段性记录（2026-08-21 更新）

> 目标：本机（RTX 2000 Ada 8GB / WSL2）零成本复现论文核心主张（Gemma3 1B 上 recirculation 显著降困惑度）。

### ✅ 已完成

1. **环境**：WSL2 + RTX 2000 Ada 8GB（CUDA 13.0），torch 2.11+cu130，transformers 4.57.6；agent shell 无 GPU 透传（会话级限制），实验由用户在 GPU 终端执行 `run_gpu.sh`。
2. **模型**：`google/gemma-3-1b-pt` 为 **gated 模型**（需 HF token，已用用户 token 下载 1GB 权重到 `.hf_cache/`）；26 层、hidden 1152、4 heads、head_dim 256、sliding_window 512（每 6 层一个 full_attention，其余 sliding_attention）。
3. **实现**：`recirculation.py` 手写 PyTorch 顺序前向（未用模型封装 forward）：
   - 逐 token 两遍前向：第一遍增量前向（复用 KV cache）记录各层残差；第二遍按公式 (1)(2) 混合后重算 dest..top 层；
   - 自研 `OverwriteCache`（包装 DynamicCache）：第一遍追加 KV，第二遍覆盖 dest..top 层 KV；
   - 每步按 cache 状态重新生成 causal/sliding mask；rotary 预计算后按位置切片；
   - 1B ramping（前 10 token 线性起调 α）；
   - 排坑记录：embed_tokens 需 2D 输入、Gemma3 层需外部传 position_embeddings_global/local、decoder layer 不返回 KV（靠 Cache 对象内部更新）、eager attention 自行截取 mask 等。
4. **正确性验证通过**：α=0 时 recirculation 与 baseline 困惑度完全一致（GPU 上相对差 **0.0028%**），证明增量路径实现正确。

### ✅ 关键正面结果（PG-19 真实数据，2026-08-21 晚更新）

`emozilla/pg19`（parquet 镜像，规避 datasets 5.0 脚本限制）test 前 2 个长文档开头 1024 token：

- baseline ppl = **19.13**（接近论文 arXiv 的 19.10，校准良好）
- **α=0.07, src=11, dst=4 → −0.29%**（最优，正是论文表 B.1 的 1B 配置）
- α=0.07, src=10, dst=4 → −0.24%；α=0.07, src=12, dst=5 → −0.05%
- α=0.10 表现更差（+0.05% ~ +0.19%），**α 越小越好**——与论文扫描一致
- 逐文档一致改进（doc0: 9.471→9.422，doc1: 38.630→38.605）

**三个与论文一致的定性结论全部复现**：真实数据上 recirc 降 ppl；{11,4} 最优层对；α=0.07 优于 0.10。
收益幅度（−0.3%）小于论文 −14%，原因：仅 2 文档、1024 token 窗口、统计噪声大；论文为全 test set 平均。

### ✅ 严肃验证结果（PG-19 多文档 × 多位置窗口，2026-08-22 更新）

`eval_strict.py`：6 个 PG-19 文档 × 每文档 3 个位置窗口 = 18 窗口 × 1024 token：

| 配置 | ppl 变化 | 窗口胜出率 |
|---|---|---|
| **α=0.07, 12-5** | **−7.93%** | 13/18 (72%) |
| α=0.07, 10-4 | −4.59% | 15/18 (83%) |
| α=0.07, 11-4 | −4.28% | 15/18 (83%) |
| α=0.04, 12-5 | −5.43% | 14/18 (78%) |
| α=0.04, 10-4 | −3.33% | 14/18 (78%) |
| α=0.04, 11-4 | −3.28% | 16/18 (89%) |

- **全部 6 配置一致降 ppl，胜出率 72%~89%**——收益普遍稳定
- 多位置窗口放大收益（此前只用文档开头仅 −0.29%）：**−3.3%~−7.9%**
- α=0.07 优于 0.04（与论文"α 不宜过大"一致，但 0.07 仍优于 0.10）
- 最优层对 {12,5}（论文表 B.1 全 test set 最优为 {11,4}，子集上相邻层对更优合理）
- 逐位置诊断：后段桶增益增强（pos 896-1022: +0.0196），符合论文图 9"收益随 lag 增长"机制（单窗口有波动）
- baseline ppl = 27.30（多位置窗口拉高了基线，因包含文档中后部更难位置）

**判定：✅ 复现成功**（证据链：α=0 一致性 → 全配置降 ppl → 高胜出率 → 位置增益增强 → 最优层对相邻）

### ⚠️ 阶段性负面结果（内置重复文本测试集，已定位为测试集问题）

| 测试集 | 窗口 | baseline ppl | α=0.07 | α=0.10 | α=0.15 |
|---|---|---|---|---|---|
| 内置重复文本（BUILTIN_TEXT×4） | 1024 | 1.861 | +0.81% | +1.32% | +3.10% |
| 内置文本 | 512 | 3.432 | — | — | +5.07% |
| 内置文本 | 200 | 3.279 | — | — | +8.13% |

- 所有配置均**正 ppl（负收益）**，且 α 越大越差 → 模式上更像"收益未出现"而非实现错误。
- 逐位置诊断（1024 窗口 α=0.10）：pos 0-63 +0.02，pos 256-383 **-0.07~-0.10**（显著负增益），之后趋近 0。
- **推断**：内置重复文本是糟糕的测试集（ppl 仅 1.86，模型对重复内容几乎确定，无跨句状态可跟踪；负增益段恰为重复文本第二段开头）。论文收益来自真实长文档（PG-19 等，ppl ~22）。

### 🔜 下一步（可选增强）

- 扩大样本：更多 PG-19 文档（`--n_docs 10~20`）以降低噪声、逼近论文幅度（−14% 需要全 test set + 多位置窗口）
- 论文 4.4.4 的逐位置/逐词类诊断可复刻
- 若需更大收益：尝试更小 α（0.04）、更多层对、或 4B 模型（论文 4B 收益更大）

### 备注

- 论文关键超参：Gemma3 1B {src=11, dst=4}（HF 0-based），α 常用 0.07/0.15，1B 用凸混合 β=1−α，ramping 前 10 token。
- 相关文件：`recirculation.py`（核心实现）、`eval_ppl.py`（困惑度评估，内置文本/pg19）、`eval_real.py`（PG-19 真实文档扫描）、`run_gpu.sh`（GPU 一键脚本）、`check_gpu.py`（环境自检）。
