# 复现计划与演变记录：Recirculation（arXiv:2608.17981）

> **状态：✅ 已完成（2026-08-21 晚至 08-22 凌晨）**——本机 WSL2 + RTX 2000 Ada 上成功复现核心主张（PG-19 上 recirc 一致降困惑度，最优 −7.93%）。
> 本文档同时记录**当初的计划**与**实际执行的差异**（下文每节附「实际 vs 计划」），重点说明从"免费云端 T4"到"本地 GPU"的方案转变。
> 完整复现报告见 [`reading.md`](reading.md)；实验数据见结果库 `results.json`（实验 A/B/C）。

## 0. 原始计划（2026-08-21 制定）

> 目标：以 ≈$0 成本、1–2 天墙钟时间，验证论文核心主张（训练无关的推理期 recirculation 能显著降低困惑度）。
> 官方未发布代码；社区仅有 toy-scale 参考（avikds/recirculation-from-scratch，非官方，仅作机制参考——实际未使用其代码）。

**当时的成功标准**（对应论文 Table 1，Gemma3 1B PT）：

| 指标 | 论文数值 | 计划成功标准 |
|---|---|---|
| baseline 困惑度（PG-19） | 22.27 | ≈22 ± 2（校验实现正确） |
| recirculation 困惑度 | 19.06（−14.4%） | 降低 ≥ 8%（窗口更短时略小） |
| arXiv（1B） | 19.10 → 16.54（−14.0%） | 降低 ≥ 8% |

内建校验：baseline 困惑度必须与论文同数量级，否则说明数据/实现有偏差。

**计划要点**：

- **硬件方案（三档，选 A）**：A=Colab/Kaggle 免费 T4 16GB（$0）；B=本地 3090/4090；C=租 A10/4090 云实例（$2–6/时）。
- **复现范围**：P0 核心 = Gemma3 1B 在 PG-19/arXiv 上的 ppl 对比；P1 敏感性 = α∈{0.04,0.07,0.10,0.15} × 6–8 个层对的小网格；P2/P3 可选 = GSM8k、instruction-following、adaptive（MLP+BPTT）。
- **数据与窗口**：PG-19（长文，效果最显著）；窗口 512 token 起步（论文用 1024，成本减半），P0 确认后再上 1024。
- **工作量估计**：M0 环境+前向实现 2–3h → M1 baseline 校准 0.5–1h → M2 核心实验（300–500 窗口）2–4h → M3 敏感性网格（100 窗口）2–3h → 总计 5–10 GPU 小时。
- **实现方式**：手动逐层循环 model.layers，两遍前向；全程 `torch.no_grad()`；层对 {源=11, 目标=4}（论文表 B.1）；前 10 token ramping。

---

## 1. 方案演变：云端 T4 → 本地 GPU（关键变化）

**实际执行：完全放弃云端，改用本机 WSL2 的 RTX 2000 Ada Laptop 8GB（CUDA 13.0），$0 硬件成本，GPU 总耗时约 2.5 小时（08-21 晚一次完成）。**

| 维度 | 原始计划 | 实际执行 | 变化原因 |
|---|---|---|---|
| 硬件 | Colab/Kaggle 免费 T4（档位 A） | 本机 RTX 2000 Ada 8GB（WSL2） | 本机已有可用 GPU（相当于计划档位 B 的降级版，8GB 显存 + bf16 足够跑 Gemma3 1B）；免去云端环境配置与断连风险 |
| 执行方式 | 在 Colab notebook 分步跑 | git 仓库内脚本 + `run_gpu.sh` 一键跑（用户 GPU 终端执行） | agent shell 无 GPU 透传（会话级限制），代码与结果全部落在本地仓库，实验由用户终端执行 |
| 模型 | Gemma3 1B PT（gated，需 HF token） | 同左；权重缓存在本地 `.hf_cache/`（约 1GB），`HF_TOKEN` 从环境变量读取 | 无变化 |
| 数据获取 | 官方 `deepmind/pg19`（脚本数据集） | `emozilla/pg19`（parquet 镜像，streaming 流式读取） | **计划外现实**：新版 datasets 5.x 不再支持旧式脚本数据集，官方仓库直接加载失败；parquet 镜像可流式读取且无需下载 11GB 全量 |
| 窗口规模 | 300–500 窗口（M2） | 实验 A：2 文档×开头 1024；实验 B：6 文档×3 位置=18 窗口×1024 | recirc 是顺序前向（每 token 两遍），T4 级 300–500 窗口估计过于乐观；8GB 卡上 18 窗口已足够得出统计结论 |
| 窗口长度 | 512 起步，P0 后决定是否上 1024 | 直接用 1024（与论文一致） | 短窗效果弱（负面对照实测 200/512 token 上 recirc 有害），且单窗口成本可接受 |
| α 网格 | {0.04, 0.07, 0.10, 0.15} | {0.04, 0.07, 0.10}（0.15 仅出现在负面对照） | 实验 B 中 0.07>0.04 已确立趋势；0.15 在论文中已偏大，且重复文本对照已含 0.15 |
| 层对网格 | 6–8 个 | 3 个（11-4, 10-4, 12-5）+ 实验 A 同 3 个 | 论文最优 {11,4} 及相邻对已覆盖，足以验证"最优层对相邻" |
| 数据集 | PG-19 + arXiv（+C4 可选） | 仅 PG-19（真实长文）；内置重复文本仅作负面对照 | arXiv/C4 的流式读取与 PG-19 同构，核心主张已由 PG-19 验证；论文的 arXiv 数值仅用于 baseline 校准（19.13 vs 19.10 ✅） |
| P2/P3（GSM8k、instruction、adaptive） | 可选扩展 | **未做** | P0 结论已达成，按"最低成本"原则收尾 |
| 交付物 | `recirculation_forward.py` + `eval_ppl.py` + `sweep.py`（热力图） | `recirculation.py` + `eval.py`（统一入口，JSON 输出，无热力图） | 实际迭代中评估脚本三合一（见 §3）；热力图以结果表/JSON 替代 |

## 2. 实际执行步骤（与计划的对应）

| 步骤 | 实际内容 | 对应计划 | 实际耗时 |
|---|---|---|---|
| 1 | 写核心实现 `recirculation.py`：顺序 prefill + 两遍前向 + `OverwriteCache`（见 §3 排坑） | M0 | 含在 2.5h 内 |
| 2 | 负面对照先行：内置重复文本上 α∈{0.07,0.10,0.15}（结果全部正收益，ppl 1.86 无可跟踪状态）→ **教训：必须用真实长文档** | M0 调试 | 含在内 |
| 3 | 实验 A：2 文档 × 开头 1024 token，baseline=19.13（≈论文 arXiv 19.10，**校准通过**），6 配置仅 −0.29%~+0.19% | M1（校准）+ M2 缩水版 | 含在内 |
| 4 | 发现开头窗口收益弱 → 新增 `eval_strict.py`：多文档 × 多位置窗口（收益随 lag 增长） | M2 修正 | 含在内 |
| 5 | 实验 B：6 文档 × 3 位置 = 18 窗口 × 1024，6 配置**全部降 ppl**（−3.28%~−7.93%，最优 α=0.07/12-5，胜出率 72–89%）+ 逐位置诊断（后段增益 +0.0196） | M2/M3 | 含在内 |
| 6 | 整理报告（reading.md / repro_plan.md 摘要）与开源准备（README/License/requirements/CI/smoke_test/结果环境指纹） | M5 后收尾 | 08-21 23:00 后 |
| 7 | （08-24）合并三个 eval 脚本为 `eval.py` | 交付物整理 | — |

**计划外新增（实际比计划多做/多遇到的）**：
- `OverwriteCache`（自定义 KV cache 覆盖模式）——计划未预见；不加它后续 token 会看到未 recirculate 的状态
- Gemma3 双注意力（full + sliding 512）掩码逐类型生成、RoPE 按层类型分发、eager attention 自截 mask——计划写"手动逐层循环"但未料到 Gemma3 层接口的这些细节
- 兼容 transformers 4.x/5.x 两代层 API（`position_embeddings_global/local` vs `layer_type`）
- `smoke_test.py`（tiny-random Gemma3，CPU ~10s，CI 用）与 `get_env_fingerprint()`（结果 JSON 带环境指纹）

## 3. 实现要点（最终形态）

按论文公式 (1)(2)，对每个窗口做**顺序 prefill**（不能用标准并行 prefill）：

```
对窗口内每个 token t（顺序执行）：
  1. 第一遍：正常前向（复用已 recirculate 的 KV cache），
     记录 layer s 与 layer d 的残差流输出 z_s、z_d
  2. 归一化：f(z_s) = z_s × ||z_d||₂ / ||z_s||₂
  3. 混合（1B 用凸混合 β=1−α_t，α_t 前 10 token 线性 ramp）：
     z_d' = α_t·f(z_s) + (1−α_t)·z_d
  4. 第二遍：只重算 layer d..top（layer d 输入换成 z_d'），
     预测概率取第二遍输出；把第二遍的 K/V 存入 cache
```

- **层索引**：论文表 B.1 {源=11, 目标=4}，HF 0-based 直接对应（脚注 4：PyTorch/HF 复现与 JAX 结果一致）；实测最优 {12,5} 与之相邻
- **KV cache**：自研 `OverwriteCache`（子类化 DynamicCache）——第一遍追加模式、第二遍覆盖 dest..top 层模式
- **实现方式**：手动逐层循环 model.layers；全程 `torch.no_grad()`；兼容 Gemma3 的 full/sliding 双注意力与 4.x/5.x 层 API
- **总开销**：每 token ≈ 2 次前向（第二遍只算 layer d..top），整窗 ≈ 2× 普通 prefill 的 FLOPs，无 T² 灾难

## 4. 结果与对照（最终）

**结论：✅ 复现成功**（详见 reading.md 完整报告）。

| 实验 | 配置 | baseline | 最优 recirc | 说明 |
|---|---|---|---|---|
| A（2 文档×开头 1024） | α=0.07/11-4 | 19.13 | −0.29% | baseline ≈ 论文 arXiv 19.10，校准通过；开头窗口收益弱 |
| B（6 文档×3 位置=18 窗口×1024） | 6 配置全扫 | 27.30 | **−7.93%**（α=0.07/12-5，胜出 13/18） | 全部配置一致降 ppl，胜出率 72–89% |

- 正确性：α=0 一致性 0.0028%（纯浮点噪声）
- 负面对照：内置重复文本（ppl 1.86）上收益全为负——测试集无状态可跟踪，非实现问题
- 与论文对照：方向一致 ✅ / 最优层对相邻 ✅ / α 趋势一致 ✅ / 位置机制一致（后段增益 +0.0196）✅ / baseline 同量级 ✅；幅度 −7.9% < 论文 −14.4%（18 窗口 vs 全 test set 的样本量差异）

**成功标准达成情况**：baseline 校准 ✅（19.13 vs 论文 19.10/22.27 同量级）；降幅达标 ⚠️（−7.9% 接近但未达计划 ≥8%——论文全 test set 的 −14.4% 需更大样本，阅读.md §5 有讨论；方向上完全一致且胜出率 72–89% 稳健）。

## 5. 风险与缓解（实际验证）

| 计划风险 | 实际结果 |
|---|---|
| 层索引偏移 | 未发生：{11,4} 直接可用，最优 {12,5} 相邻 |
| 效果不出现 | **真实发生（短窗/重复文本）**→ 缓解有效：换真实长文档 + 长窗口 + 多位置后收益出现 |
| T4 显存不足 | 未遇到：8GB 卡 + bf16 跑 1024 窗口无压力 |
| 免费 GPU 会话断连 | 未遇到（本地 GPU）；结果 JSON 分步落盘的设计保留 |
| 长文效果 vs 短文 | **真实发生**：200/512 token 与重复文本均无收益，1024 + 真实长文档才有 → 与论文"短序列无收益"一致 |
| **计划外**：datasets 5.x 不支持脚本数据集 | 换 `emozilla/pg19` parquet 镜像流式读取（README 有说明） |
| **计划外**：transformers 4.x/5.x API 差异 | recirculation.py 双兼容 + requirements 锁定 `<5` + CI 实测 |

## 6. 交付物（最终）

- `recirculation.py`：核心实现——顺序 prefill + recirculation 两遍前向 + `OverwriteCache` + 困惑度评估（教学式注释）
- `eval.py`：统一评估入口——多数据集（builtin/pg19/arxiv/c4）、多文档×多位置窗口、α × (源,目标) 网格扫描、逐位置诊断（对照论文图 9）；结果以 JSON 输出（未生成热力图，原计划 `sweep.py` 的落点）
- `run_gpu.sh` / `check_gpu.py`：GPU 一键脚本（快速验证 + --full 完整实验）/ 环境自检
- `smoke_test.py`：无 GPU 冒烟自检（CI 用）；`.github/workflows/ci.yml`：GitHub Actions CI
- `results.json`：实验结果库（实验 A/B/C 原始数据，含环境指纹；`eval.py --append` 继续追加）
- 结果表 + 与论文 Table 1 的对照（`reading.md` §4 + README）
- git 历史：`9dc0897` 初始 → `f27825d` 注释润色 → `9f994bb` 设备修复 → `b347d0d` 实验 B 成功 → `81078fd` 报告 → 开源准备（`a165ff1` 起）→ `a65e1e8` eval 脚本合并

**一句话总结**：原计划用免费 Colab T4 分步跑 300–500 窗口；实际改为本机 WSL2 RTX 2000 Ada 一次跑完（约 2.5 GPU 小时、$0），经历"短窗无收益→换真实长文档"的关键修正后，18 窗口上 6 配置一致降困惑度（最优 −7.93%），成功复现论文核心主张。
