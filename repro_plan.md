# 最低成本复现方案：Recirculation（arXiv:2608.17981）

> 目标：以 ≈$0 成本、1–2 天墙钟时间，验证论文核心主张（训练无关的推理期 recirculation 能显著降低困惑度）。
> 官方未发布代码；社区仅有 toy-scale 参考（avikds/recirculation-from-scratch，非官方，仅作机制参考）。

## 0. 复现目标与成功标准（对应论文 Table 1）

| 指标 | 论文数值（Gemma3 1B PT，pg19） | 我们的成功标准 |
|---|---|---|
| baseline 困惑度 | 22.27 | ≈22 ± 2（校验实现正确） |
| recirculation 困惑度 | 19.06（−14.4%） | 降低 ≥ 8%（窗口更短时略小） |
| arXiv（1B） | 19.10 → 16.54（−14.0%） | 降低 ≥ 8% |

方法本身的**内建校验**：baseline 困惑度必须与论文同数量级，否则说明数据/实现有偏差。

## 1. 预算与硬件（三档，选最低档即可）

| 档位 | 硬件 | 成本 | 说明 |
|---|---|---|---|
| **A（推荐）** | Colab 免费 T4 16GB / Kaggle T4 | $0 | Gemma3 1B bf16 ≈ 2.5GB，足够 |
| B | 本地 3090/4090 | ~$0 | 速度快 3–5×，sweep 更宽裕 |
| C | 租 A10/4090 云实例 | ~$2–6/小时 | 需要跑 4B 或大 sweep 时 |

**结论：Gemma3 1B 一个模型就足够验证核心主张**，免费 T4 即可。4B/12B、GSM8k、adaptive 变体均为可选扩展。

## 2. 复现什么（按优先级）

- **P0 核心**：Gemma3 1B PT 在 PG-19（或 arXiv 子集）上的困惑度，baseline vs recirculation。
- **P1 敏感性**：α ∈ {0.04, 0.07, 0.10, 0.15} + 少量 (源,目标) 层对的小网格 → 复现"热图平滑、存在改善区域"的定性模式。
- **P2 可选**：GSM8k zero-shot（recirculation vs baseline，pass@1）；instruction-following 任务（数据可自生成，成本极低）。
- **P3 可选（最贵）**：adaptive recirculation（小 MLP + BPTT）——仅当 P0 复现成功再做。

## 3. 实现要点（方法非常简，核心 ~100 行）

按论文公式 (1)(2)，对每个窗口做**顺序 prefill**（不能用标准并行 prefill）：

```
对窗口内每个 token t（顺序执行）：
  1. 第一遍：正常前向（复用已 recirculate 的 KV cache），
     记录 layer s 与 layer d 的残差流输出 z_s、z_d
  2. 归一化：f(z_s) = z_s × ||z_d||₂ / ||z_s||₂
  3. 混合（1B 用凸混合 β=1−α）：
     z_d' = α·f(z_s) + (1−α)·z_d      # α=0.15（论文 Table 1 配置）
  4. 第二遍：只重算 layer d..top（layer d 输入换成 z_d'），
     预测概率取第二遍输出；把第二遍的 K/V 存入 cache
```

- **层索引**：Gemma3 1B 用论文表 B.1 的 {源=11, 目标=4}（HF 0-based 需先探测确认偏移——论文脚注 4 说明 PyTorch/HF 复现与 JAX 结果一致，映射应为直接对应）。
- **1B 专属**：前 10 个 token 用 ramping `α_t = min(t/10,1)·α`（附录 B.3）。
- **实现方式**：手动逐层循环 model.layers（第一遍 + 第二遍），完全控制残差流，避免 hook 的坑；全程 `torch.no_grad()`（P0/P1 不需梯度）。
- **总开销**：每 token ≈ 2 次前向（第二遍只算 layer d..top），整窗 ≈ 2× 普通 prefill 的 FLOPs，无 T² 灾难。

## 4. 执行步骤与工作量

| 步骤 | 内容 | 耗时（T4 级） |
|---|---|---|
| M0 | 搭环境（PyTorch + transformers + datasets），写顺序 prefill + recirculation 前向；用 2–3 个窗口跑通，确认无 NaN、logits 有限 | 2–3 小时（含调试） |
| M1 | **关键校验**：baseline ppl 应 ≈ 论文值（pg19 ≈22、arXiv ≈19）；偏差 >10% 先修实现 | 0.5–1 小时 |
| M2 | P0 核心实验：2–3 个数据集 × 300–500 窗口（512 token），baseline vs recirc | 2–4 小时 |
| M3 | P1 敏感性：小网格（α×4，层对 6–8 个）在 100 窗口上评估 | 2–3 小时 |
| M4 | （可选）GSM8k / instruction-following | 1–3 小时 |
| M5 | （可选）adaptive：窗口 256、100 步、batch 32，MLP 训练（BPTT） | 半天级 |

**数据**：PG-19（长文，效果最显著）；arXiv/C4 子集可选。窗口 512 token 起步（论文用 1024；短窗效果略弱但成本减半——P0 确认后再决定是否上 1024）。

## 5. 风险与缓解

| 风险 | 缓解 |
|---|---|
| 层索引偏移（HF vs 论文） | M1 用 baseline ppl 校准；再不行做小探测（看哪层输出已消歧） |
| 效果不出现 | 检查：α 是否太小（0.15）、窗口是否够长（>256）、归一化是否漏了、KV cache 是否取自第二遍 |
| T4 显存不足 | bf16/fp16；窗口 512；必要时梯度 checkpoint 无关（无梯度） |
| 免费 GPU 会话断连 | 检查点化：每跑完一个配置存 ppl 结果，脚本可续跑 |
| 长文效果 vs 短文 | 论文明示 lambada（短文）无收益——**别用短序列验证**，选 PG-19/arXiv |

## 6. 交付物

- `recirculation_forward.py`：顺序 prefill + recirculation 前向（可复用于所有实验）
- `eval_ppl.py`：数据集窗口化 + baseline/recirc 困惑度对比
- `sweep.py`：α 与 (源,目标) 小网格扫描，输出热力图
- 结果表 + 与论文 Table 1 的对照

**一句话**：用免费 Colab T4 跑 Gemma3 1B，先以 baseline 困惑度校准实现，再做 PG-19/arXiv 上 baseline vs recirculation 对比（期望 −8%~−15%），总计算量约 5–10 GPU 小时、成本 $0。

---

## 实施进度（2026-08-21 更新）

### 已落地（与原始计划的主要差异）

| 计划项 | 实际状态 |
|---|---|
| 硬件 | 未用 Colab，改用**本机 RTX 2000 Ada 8GB / WSL2**（CUDA 13.0） |
| 模型 | `google/gemma-3-1b-pt` 为 **gated 模型**，已用用户 HF token 下载（1GB 到 `.hf_cache/`） |
| 实现 | `recirculation.py` 完成：手写 PyTorch 顺序前向 + `OverwriteCache`（KV 覆盖）+ Gemma3 sliding mask + ramping |
| 正确性校验 | ✅ **α=0 一致性通过**（GPU 相对差 0.0028%），增量路径实现正确 |
| P0 初步实验 | ⚠️ 内置文本上**未复现收益**（见下），待换 PG-19 真实数据验证 |

### ✅ 关键正面结果（PG-19 真实数据）

`emozilla/pg19` test 前 2 文档 × 1024 token：

| 配置 | ppl 变化 |
|---|---|
| **α=0.07, src=11, dst=4** | **−0.29%**（最优，= 论文表 B.1 的 1B 配置） |
| α=0.07, src=10, dst=4 | −0.24% |
| α=0.07, src=12, dst=5 | −0.05% |
| α=0.10, src=11, dst=4 | −0.20% |
| α=0.10, src=10, dst=4 | +0.05% |
| α=0.10, src=12, dst=5 | +0.19% |

- baseline ppl=19.13（接近论文 arXiv 19.10，校准良好）
- **复现了论文三个定性结论**：真实数据降 ppl；{11,4} 最优层对；α=0.07 优于 0.10
- 幅度 −0.3% < 论文 −14%：仅 2 文档、窗口开头、噪声大；扩大样本可逼近

### ✅ 严肃验证结果（PG-19 多文档 × 多位置窗口）

`eval_strict.py`：6 文档 × 3 位置 = 18 窗口 × 1024 token：

| 配置 | ppl 变化 | 窗口胜出率 |
|---|---|---|
| **α=0.07, 12-5** | **−7.93%** | 13/18 (72%) |
| α=0.07, 10-4 | −4.59% | 15/18 (83%) |
| α=0.07, 11-4 | −4.28% | 15/18 (83%) |
| α=0.04, 12-5 | −5.43% | 14/18 (78%) |
| α=0.04, 10-4 | −3.33% | 14/18 (78%) |
| α=0.04, 11-4 | −3.28% | 16/18 (89%) |

**判定：✅ 复现成功**——全配置降 ppl、胜出率 72-89%、位置增益后段增强、最优层对 {12,5} 与论文 {11,4} 相邻。

### ⚠️ 关键负面结果（内置重复文本测试集，已定位为测试集问题）

| 窗口 | baseline ppl | α=0.07 | α=0.10 | α=0.15 |
|---|---|---|---|---|
| 1024 | 1.861 | +0.81% | +1.32% | +3.10% |
| 512 | 3.432 | — | — | +5.07% |
| 200 | 3.279 | — | — | +8.13% |

- 所有 α 均为正 ppl（负收益），α 越大越差；逐位置诊断显示 pos 256-383 显著负增益（对应重复文本第二段开头）。
- **推断**：内置重复文本 ppl 太低（1.86）、模型已"背熟"，无跨句状态可跟踪 → 换 **PG-19 真实书籍**（论文同款，ppl ~22）验证。

### 🔜 下一步命令（GPU 终端执行，可选增强）

```bash
# 扩大样本降低噪声（约 1-2 小时）：10 文档 × 最优配置 + 更小 α
python3 eval_real.py --n_docs 10 --alphas 0.04 0.07 --layer_pairs 11-4 10-4 12-5
```

判读：n_docs=10 时若 −0.5%~−2%（随样本增大噪声下降），可进一步上 20+ 文档逼近论文幅度。

### 已交付文件

- `recirculation.py`：核心实现（顺序 prefill + 公式 1/2 混合 + ramping + KV 覆盖缓存）
- `eval_ppl.py`：困惑度评估（内置文本 / pg19 / arxiv / c4，窗口化）
- `eval_real.py`：PG-19 真实文档扫描（多 α × 多层对，streaming 免全量下载）
- `run_gpu.sh`：GPU 一键脚本（quick/full 两模式，无 GPU 快速失败）
- `check_gpu.py`：环境自检
